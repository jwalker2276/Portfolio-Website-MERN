// Default state
const authState = {
  isAuth: false,
  user: {}
};

// Check for action type
const reducer = (state = authState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
