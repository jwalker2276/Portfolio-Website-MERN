import axios from 'axios';
import actions from './types';

// Set profile data
// export const setProfileData = profileData => {
//   axios.post('/profile', profileData).then(res => res.status);
// };
